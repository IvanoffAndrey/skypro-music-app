import styled from "styled-components";

const MenuNav = styled.div`
  display: block;
  visibility: visible;
`;

const MenuList = styled.ul`
  padding: 18px 0 10px 0;
`;

const MenuItem = styled.li`
  padding: 5px 0;
  margin-bottom: 16px;
`;

const MenuLink = styled.a`
  color: #ffffff;
  font-weight: 400;
  font-size: 16px;
  line-height: 24px;
`;

export { MenuNav, MenuList, MenuItem, MenuLink };
